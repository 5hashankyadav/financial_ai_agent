version: '3.8'

services:
  financial-ai-agent:
    build: .
    container_name: financial_ai_agent
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    env_file:
      - .env
    restart: unless-stopped
